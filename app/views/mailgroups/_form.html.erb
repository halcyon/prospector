<%= form_for(@mailgroup) do |f| %>
  <% if @mailgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailgroup.errors.count, "error") %> prohibited this mailgroup from being saved:</h2>

      <ul>
      <% @mailgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
